-p radicale-mn_{{debian_version.version}}.deb
-n radicale-mn
--provides radicale
-v "{{debian_version.version}}"
-m "Jonas Maurus"
-d python3
-d python3-pip
-d python3-virtualenv
-d adduser
# this package can't be used together with Debian's radicale package
--conflicts radicale
--before-remove .gopythongo/pre_rm.sh
--after-install .gopythongo/post_install.sh
--after-remove .gopythongo/post_rm.sh
--directories "{{basedir}}"
--directories /run/radicale
# disabled --deb-systemd since FPM does not honor policy-rc.d in it's default scripts
# --deb-systemd .gopythongo/etc/systemd/radicale.service
{{basedir}}
.gopythongo/etc/radicale/=/etc/radicale/
.gopythongo/radicale.tmpfilesd.conf=/usr/lib/tmpfiles.d/radicale.conf
.gopythongo/etc/systemd/radicale.service=/lib/systemd/system/radicale.service
